# =============================================================================
#  driver.mk — {{ target_name }}
#  Generated by new-target.py on {{ generated_date }}
# =============================================================================

# ── Primary simulation driver ─────────────────────────────────────────────────
DRIVER_CC := $(target_base_dir)/src/main/cc/firesim_top.cc

# ── Bridge C++ sources (populated by opt-in fragments in config.mk) ───────────
DRIVER_CC += $(COMMON_DRIVER_CC)

# ── Include paths ─────────────────────────────────────────────────────────────
# $(COMMON_CXX_FLAGS) carries -I paths and -DENABLE_* defines from fragments.
# $(GENERATED_DIR) is needed for GoldenGate-generated *_struct.h headers.
TARGET_CXX_FLAGS := \
  $(COMMON_CXX_FLAGS) \
  -I$(target_base_dir)/src/main/cc \
  -I$(GENERATED_DIR)

TARGET_LD_FLAGS :=
