[project]
name = "firesim-lab"
dynamic = ["version"]

[project.scripts]
init-target = "fslab.init_target:main"

[tool.setuptools.packages.find]
where = ["."]

[tool.setuptools.package-data]
firesim_lab = ["templates/*.j2"]