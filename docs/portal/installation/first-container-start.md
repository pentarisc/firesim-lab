# First Container Start

```{note}
Placeholder — content pending. See `docs/prompts/docs-portal-handoff.md` for the drafting workflow.

Walks through the first-time container start: pulling the image, launching with docker-compose, verifying the `fslab` CLI is on PATH, and confirming the toolchain is wired up.
```
