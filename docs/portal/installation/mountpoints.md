# Mountpoints

```{note}
Placeholder — content pending. See `docs/prompts/docs-portal-handoff.md` for the drafting workflow.

Explains how the user's working directory and cache locations on the host map to paths inside the container (bind mounts per `docker/docker-compose-dev.yaml`, `/target` convention, etc.).
```
