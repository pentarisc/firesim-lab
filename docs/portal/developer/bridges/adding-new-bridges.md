# Adding New Bridges

```{note}
Placeholder — content pending. See `docs/prompts/docs-portal-handoff.md` for the drafting workflow.

Step-by-step: defining the bridge in `lib/`, wiring a registry entry, adding Jinja2 templates under `fslab-cli/fslab/templates/bridges/`, exposing port mappings to `fslab init`/`generate`.
```
