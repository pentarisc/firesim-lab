# registry.yaml Reference

```{note}
Placeholder — content pending. See `docs/prompts/docs-portal-handoff.md` for the drafting workflow.

Full schema reference for `lib/registry.yaml`: every field, its meaning, and how it is consumed by `fslab generate`.
```
