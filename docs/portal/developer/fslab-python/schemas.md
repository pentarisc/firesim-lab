# Schemas

```{note}
Placeholder — content pending. See `docs/prompts/docs-portal-handoff.md` for the drafting workflow.

Internal Python schemas backing `fslab.yaml` and `lib/registry.yaml`: validation rules, model classes, how parsed objects flow into template rendering.
```
