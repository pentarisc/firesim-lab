# Docker Architecture

```{note}
Placeholder — content pending. See `docs/prompts/docs-portal-handoff.md` for the drafting workflow.

Covers: image layout, base image choices, what is baked in vs mounted, cache mount strategy, devcontainer wiring, the role of `docker/docker-compose-dev.yaml`.
```
