# firesim-lab AWS Setup

```{note}
Placeholder — content pending. See `docs/prompts/docs-portal-handoff.md` for the drafting workflow.

Covers: firesim-lab-specific AWS prerequisites — F2 quota requests, VPC/subnet, S3 bucket layout, IAM roles/policies, key pairs, and any other resources `fslab` expects to exist before `fslab sim fpga` works. Absorbs existing `docs/aws*.md` notes.
```
