# Quickstart: AXIUARTPrinter End-to-End

```{note}
Placeholder — content pending. See `docs/prompts/docs-portal-handoff.md` for the drafting workflow.

Copy-paste verbatim walkthrough taking the AXIUARTPrinter example from scaffold through metasim and (optionally) FPGA run. Minimal explanation; deep links into {doc}`/commands/index` for detail. Assumes {doc}`/setup/index` and {doc}`/installation/index` are complete.
```
