# fslab build

```{note}
Placeholder — content pending. See `docs/prompts/docs-portal-handoff.md` for the drafting workflow.

Covers `fslab build`: Chisel/FIRRTL generation, Golden Gate elaboration, and the metasim/driver/FPGA build paths. Use AXIUARTPrinter as the running example.

**Split candidate:** if this page grows past ~2000 words, propose splitting into a `commands/build/` subfolder (e.g. `index.md`, `stages.md`, `metasim.md`, `fpga.md`).
```
