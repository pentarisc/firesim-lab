# fslab monitor

```{note}
Placeholder — content pending. See `docs/prompts/docs-portal-handoff.md` for the drafting workflow.

Covers `fslab monitor build | run`: attaching to an in-flight background build or detached run.
```
