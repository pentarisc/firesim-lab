# fslab new

```{note}
Placeholder — content pending. See `docs/prompts/docs-portal-handoff.md` for the drafting workflow.

Covers `fslab new <project>`: scaffolding a new out-of-tree project folder. Use AXIUARTPrinter as the running example.
```
