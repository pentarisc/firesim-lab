# fslab init

```{note}
Placeholder — content pending. See `docs/prompts/docs-portal-handoff.md` for the drafting workflow.

Covers `fslab init`: parsing the user's top-module file and generating `fslab.yaml` with design ports populated. Use AXIUARTPrinter as the running example.
```
