# fslab sim fpga

```{note}
Placeholder — content pending. See `docs/prompts/docs-portal-handoff.md` for the drafting workflow.

Covers `fslab sim fpga`: running a built bitstream on a real F2 FPGA host, in either foreground or detached (`--detach`) mode. Use AXIUARTPrinter as the running example. References {doc}`/setup/aws/firesim-lab-aws-setup` for AWS prerequisites.

**Split candidate:** if foreground vs detached flows and F2 instance lifecycle grow this page past ~2000 words, propose splitting into a `commands/sim-fpga/` subfolder.
```
